# ── Source & dev files ────────────────────────────────────────────
.claude/
.idea/
.gitignore
.prettierignore
.prettierrc

# Docs / notes
CLAUDE.md
docs/

# Go source
cmd/
internal/
go.mod
go.sum
tools/

# Build config
Makefile
tsconfig.json
tsconfig.node.json
vite.config.ts
svelte.config.js
svelte.config.mjs

# ── TypeScript source (keep only compiled output) ──────────────────
extension/src/
extension/tsconfig.json
extension/node_modules/
**/*.ts
!extension/out/**

# ── Webview source (keep only compiled output) ─────────────────────
webview/src/
webview/node_modules/

# ── Node modules ───────────────────────────────────────────────────
node_modules/

# ── Test artifacts & e2e output ────────────────────────────────────
playwright-report/
test-results/
tests/
playwright.config.ts
vitest.config.ts
vitest-setup.ts
**/*.test.ts
**/*.test.js
**/*.spec.ts

# ── Dev binary (runtime uses bin/<platform>-suffixed names) ────────
# Exact path, no wildcard — a trailing * would exclude the platform binaries.
bin/hydragit-server

# ── Dev docs / notes (not used at runtime) ─────────────────────────
IMPLEMENTED_FEATURES.md
documentation/
.vscode/